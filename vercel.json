{
  "buildCommand": "rm -rf .next && rm -rf node_modules/.cache && NODE_OPTIONS='--max-old-space-size=4096' next build",
  "installCommand": "npm ci --prefer-offline --no-audit",
  "framework": "nextjs",
  "outputDirectory": ".next",
  "env": {
    "NEXT_TELEMETRY_DISABLED": "1",
    "NODE_ENV": "production"
  },
  "regions": ["iad1"]
}
